# Mud-Puppy MI300X pod training dependencies
# Pinned for supply-chain safety. Update together and test before bumping.
#
# Installation (called from setup_pod.sh):
#   pip install -r scripts/mi300x/requirements-pod.txt
#
# Torch is installed separately from the ROCm index by setup_pod.sh to avoid
# pulling the default CUDA wheel here.

# Core training stack
transformers==4.47.1
datasets==3.2.0
peft==0.14.0
trl==0.13.0
accelerate==1.2.1

# Tokenization / serialization
safetensors==0.5.2
sentencepiece==0.2.0
tokenizers==0.21.0

# Hub + logging
huggingface_hub==0.27.1
tensorboard>=2.18.0,<3.0.0
