"""mud_puppy.tui -- REMOVED in v0.4.

The Rich-based TUI has been replaced by the Phos studio web UI
(mud-puppy-studio).  This module is intentionally empty; any attempt to
import TUIMonitor will raise ImportError so that old code fails loudly.
"""

raise ImportError(
    "mud_puppy.tui has been removed in v0.4. "
    "Use the Phos studio web UI instead (mud-puppy-studio)."
)
